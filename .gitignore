## .NET
bin/
obj/
*.user
.vs/

## Rider / VS Code
.idea/
.vscode/

## Node / Frontend (dashboard/) — Dependencies + Build-Artefakte NICHT committen (Lockfile bleibt)
node_modules/
dashboard/dist/
dashboard/*.tsbuildinfo
dashboard/tsconfig.tsbuildinfo
# tsc-Emit aus dashboard/vite.config.ts (Quelle bleibt getrackt) — reines Build-Artefakt:
dashboard/vite.config.js
dashboard/vite.config.d.ts

## OS
Thumbs.db
.DS_Store

## Secrets / local config (NICHT committen)
config/**/*.local.json
*.secrets.json

## Lokale Kilo-MCP-Config (enthaelt Hindsight-Auth-Token, NICHT committen)
.kilo/

## Lokale Marktdaten-Exporte (NIE committen; .gitkeep bleibt)
samples/sierra/raw/*.txt
samples/sierra/raw/*.csv
samples/sierra/raw/*.scid
samples/sierra/raw/*.tct
samples/atas/raw/*.txt
samples/atas/raw/*.csv
!samples/**/raw/.gitkeep
